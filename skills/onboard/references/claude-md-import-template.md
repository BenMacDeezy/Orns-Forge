## Forge

This repository uses Forge for autonomous development — its conventions,
build/test/lint commands, and state pointers live in `AGENTS.md`, re-exported
here so Claude Code loads them automatically every session (Claude Code reads
`CLAUDE.md` only; it does not auto-load `AGENTS.md` on its own — the `@import`
line below is the documented bridge).

@AGENTS.md
