---
status: draft
spec: null
created: DATE
approved-date: null
---

## Visual identity
<the chosen direction's name, one-paragraph description, and reference feel —
what it should read as (e.g. "confident fintech, not playful consumer app")>

## Token system
- Color — <palette + roles (background/surface/text/accent/state)>
- Type — <family(ies), scale, weight usage>
- Spacing — <base unit + scale>
- Radius — <scale, where each step applies>
- Shadow — <elevation scale>
- Motion — <duration/easing scale — link to `forge:motion-design-principles`
  rather than restating its rules>

## Layout language
<grid/composition rules, density, information-hierarchy conventions>

## Component patterns
<how common components — nav, cards, forms, tables, empty/loading/error
states — should look and behave in this direction>

## Interaction personality
<the motion/feedback character this direction implies; concrete choices live
in Token system > Motion, this section is the "why" and the reference feel>

## Candidate directions
<the 2-3 DISTINCT professional design directions the design lead proposed
before the human's pick — kept as a permanent record even after one is
chosen, so the rejected alternatives and reasoning survive>
1. <direction name> — <one-paragraph pitch> — <why it fits or doesn't>
2. <direction name> — <one-paragraph pitch> — <why it fits or doesn't>

## Amendments
<!-- Later specs extend or refine the foundation here, append-only. -->
<!-- ### Amendment — <date> — from <task-id> -->
(none)
