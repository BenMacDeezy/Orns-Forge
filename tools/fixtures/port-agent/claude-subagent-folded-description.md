---
name: acme-folded-agent
description: >
  A multi-line folded-scalar description, the YAML style that wraps long
  text across several indented lines instead of one long line.
model: sonnet
---

You operate normally; the only unusual thing about this fixture is that
its `description` field uses YAML's folded-scalar (`>`) style.
